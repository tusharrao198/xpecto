<!DOCTYPE html>
<html>
    <head>
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta property="og:url" content="https://xpecto.tech/" />
        <meta property="og:type" content="website" />
        <meta property="og:title" content="Xepecto'22" />
        <meta
            name="description"
            content=" Xpecto is science and technology festival of Indian
        Institute of Technology, Mandi. It has been ramped up to
        include the most electrifying events, prompting great
        discoveries, as well as enthralling brainstorming and
        grand prizes."
        />
        <title>Profile | Xpecto</title>
        <!-- CSS only -->
        <link href="./css/preload.css" rel="stylesheet" type="text/css" />
        <!-- <link href="./css/index.css" rel="stylesheet" type="text/css" /> -->
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
            crossorigin="anonymous"
        />
        <link rel="stylesheet" href="./css/navbar.css" />
        <link rel="icon" href="./images/xpecto.ico" type="image/x-icon" />
        <!-- font-awesome -->
        <link
            rel="stylesheet"
            href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
            referrerpolicy="no-referrer"
            integrity="sha512-9usAa10IRO0HhonpyAIVpjrylPvoDwiPUiKdWk5t3PyolY1cOd4DSE0Ga+ri4AuTroPR5aQvXU9xC6qOPnzFeg=="
            crossorigin="anonymous"
        />
        <style>
            /* body {
                padding-top: 70px;
            } */

            header .logo {
                font-size: 1.5rem;
            }
            .bg {
                height: 100vh;
                width: 100vw;
                background: url("../images/xpecto_tempwall.png");
                /* background: url("https://user-images.githubusercontent.com/56690827/162190335-422b76d8-99a5-4fca-97e9-5a22d3154800.png"); */
                background-color: #171717;
                position: fixed;
                z-index: -1000;
                display: flex;
                justify-content: center;
                align-items: center;
                font-size: 5.5em;
                padding: 0;
            }
            header {
                display: flex;
                padding-left: 40px;
                padding-right: 10px;
            }
            .showcase.active {
                right: 250px;
            }
            @media (max-width: 768px) {
                .showcase.active {
                    right: 50%;
                }
            }
            @media (max-width: 480px) {
                .showcase.active {
                    right: 75%;
                }
                header .logo {
                    font-size: 1.5rem;
                }
                .text-white {
                }
                header {
                    /* padding-left: 10px; */
                    padding-right: 10px;
                    display: flex;
                    /* align-items: flex-end; */
                }
                .showcase {
                    /* padding-right: 50px; */
                }
            }

            .card {
                margin-left: auto;
                margin-right: auto;
                color: white;
                font-weight: bold;
                width: 450px;
                padding: 2rem;
                backdrop-filter: blur(10px) saturate(200%);
                -webkit-backdrop-filter: blur(10px) saturate(200%);
                background-color: rgba(17, 25, 40, 0.5);
                border-radius: 12px;
                border: 2px solid rgba(255, 255, 255, 0.125);
                transition: 0.2s;
            }

            .card:hover {
                transition: 0.2s;
                box-shadow: 0 0 50px -30px #009dbe;
                backdrop-filter: blur(5px) saturate(200%);
                -webkit-backdrop-filter: blur(5px) saturate(200%);
            }
            .profile {
            }
            .profile .heading {
                font-size: 2rem;
                font-weight: bold;
                text-align: center;
            }
            .last {
                display: flex;
                justify-content: space-evenly;
                flex-wrap: wrap;
            }
            .btn {
                font-weight: bold;
                padding: 0.25rem;
                width: 100px;
                border-radius: 5px;
                margin: 0.5rem;
                transition: 0.2s;
                font-size: 1.25rem;
            }
            .back-home {
                border: 2px solid #009dbe;
                background-color: #009dbe;
                color: white;
            }
            .logout {
                border: 2px solid red;
                background-color: red;
                color: white;
            }
            .btn:hover {
                background-color: black;
                color: white;
                border-color: black;
                transition: 0.2s;
            }
            .person {
                font-size: 1.5rem;
            }
            #person-image {
                margin-bottom: 1.5rem;
            }
            .person > img {
                display: flex;
                justify-content: center;
                /* justify-items: center; */
                margin-left: auto;
                margin-right: auto;
                /* display: flex; */
                /* justify-content: center; */
            }

            .name,
            .email {
                margin: 1rem;
                font-size: 1.25rem;
            }

            .profile span {
                font-weight: 500;
            }
            .person img {
                border-radius: 50%;
            }
            .name img {
                width: 25px;
                margin-right: 0.75rem;
            }

            .email img {
                margin-right: 0.75rem;
                width: 25px;
            }

            @media (max-width: 480px) {
                .card {
                    width: 300px;
                }
                .last {
                    flex-direction: column;
                    align-items: center;
                }
                .name,
                .email {
                    font-size: 1rem;
                    display: flex;
                    flex-direction: column;

                    align-items: center;
                }
                .name > img,
                .email > img {
                    margin: 0;
                }
            }
            @media (max-width: 400px) {
                .card {
                    width: 250px;
                }
                .name,
                .email {
                    font-size: 0.9rem;
                    display: flex;
                    flex-direction: column;
                    align-items: center;
                }
                .name > img,
                .email > img {
                    margin: 0;
                }
            }

            .reg-container,
            .create-container,
            .join-container {
                display: flex;
                flex-wrap: wrap;
                justify-content: center;
            }
            @media (max-width: 480px) {
                .reg-container,
                .create-container,
                .join-container {
                    flex-direction: column;
                    align-items: center;
                    flex-wrap: nowrap;
                }
            }
            .card-event {
                margin: 0.5rem;
                color: white;
                font-weight: bold;
                /* height: 300px; */
                width: 200px;
                padding: 1rem;
                backdrop-filter: blur(10px) saturate(200%);
                -webkit-backdrop-filter: blur(10px) saturate(200%);
                background-color: rgba(17, 25, 40, 0.5);
                border-radius: 12px;
                border: 2px solid rgba(255, 255, 255, 0.125);
                /* display: flex;
                align-items: center; */
                transition: 0.25s;
                box-shadow: 0 0 50px -25px #009dbe;
            }
            .card-event:hover {
                transform: translateY(-5px);
                transition: 0.25s;
            }
            .card-event table {
                width: 100%;
                /* display: flex; */
                /* align-self: center; */
            }
            .card-event tr {
                /* margin-left: auto; */
                /* margin-right: auto; */
                display: flex;
                flex-direction: column;
                margin-bottom: 1rem;
                align-items: center;
                /* align-content: center; */
            }
            .card-event td {
                text-align: center;
                /* display: flex; */
            }
            .card-event tr:nth-child(1) {
                margin-bottom: 0.5rem;
            }
            .card-event td:nth-child(1) {
                font-size: 1.5rem;
            }
            hr {
                color: white;
            }
            .reg-heading,
            .create-heading,
            .join-heading {
                color: white;
                font-family: "Lato", sans-serif;
                font-weight: bold;
                margin-bottom: 1rem;
                text-align: center;
            }
            .members {
                text-align: center;
            }
        </style>
    </head>
    <body>
        <div class="bg"></div>
        <div class="home">
            <section class="showcase">
                <header>
                    <a href="/" class=""
                        ><img src="./images/xpecto.png" alt="" class="logo-img"
                    /></a>
                    <div class="toggle"></div>
                </header>
                <div class="overlay"></div>
                <main>
                    <div class="card">
                        <div class="profile">
                            <div class="heading">PROFILE</div>
                            <hr />
                            <div class="person">
                                <img
                                    id="person-image"
                                    src="<%= user.image %>"
                                    alt="image of person"
                                />
                                <div class="name">
                                    <img
                                        src="./icons/person.svg"
                                        alt="person-image"
                                    />
                                    <span><%= user.displayName %></span>
                                </div>
                                <div class="email">
                                    <img src="./icons/mail.svg" alt="" />
                                    <span><%= user.email %></span>
                                </div>

                                <!-- modal button  -->
                                <button id="open-modal" style="display: none">
                                    registered events
                                </button>

                                <hr />
                                <div class="last">
                                    <a href="/">
                                        <button class="btn back-home">
                                            Home
                                        </button></a
                                    >
                                    <a href="/auth/logout">
                                        <button class="btn logout">
                                            Logout
                                        </button></a
                                    >
                                </div>
                            </div>
                        </div>
                    </div>

                    <hr />
                    <!-- registered events -->
                    <h2 class="reg-heading">Registered Events</h2>
                    <div class="reg-container">
                        <% if(created_teams.length == 0) {%>
                        <button type="button" class="btn btn-info">
                            No Events Registered!
                        </button>

                        <%}else {%> <% for( let index = 0; index <
                        created_teams.length; index++ ) { %>
                        <!-- event -->
                        <div class="card-event">
                            <table>
                                <tr>
                                    <td>Event</td>
                                    <td><%= created_teams[index].id %></td>
                                </tr>
                                <tr>
                                    <td>Team</td>
                                    <td><%= created_teams[index].name %></td>
                                </tr>
                            </table>
                        </div>
                        <% } %> <% } %>
                    </div>
                    <div class="reg-container">
                        <% if(joined_teams.length == 0) {%>
                        <!-- <button type="button" class="btn btn-info">
                            No Team Joined!
                        </button> -->

                        <%}else {%> <% for( let index = 0; index <
                        joined_teams.length; index++ ) { %>

                        <!-- event -->
                        <div class="card-event">
                            <table>
                                <tr>
                                    <td>Event</td>
                                    <td><%= joined_teams[index].id %></td>
                                </tr>
                                <tr>
                                    <td>Team</td>
                                    <td><%= joined_teams[index].name %></td>
                                </tr>
                            </table>
                        </div>
                        <% } %> <% } %>
                    </div>
                    <hr />
                    <!-- registered events end -->

                    <!-- Created Teams -->
                    <h2 class="create-heading">Created Teams</h2>
                    <div class="reg-container">
                        <% if(created_teams.length == 0) {%>
                        <button type="button" class="btn btn-info">
                            No Team Created!
                        </button>

                        <%}else {%> <% for( let index = 0; index <
                        created_teams.length; index++ ) { %>

                        <!-- event -->
                        <div class="card-event">
                            <table>
                                <tr>
                                    <td>Event</td>
                                    <td><%= created_teams[index].id %></td>
                                </tr>
                                <tr>
                                    <td>Team</td>
                                    <td><%= created_teams[index].name %></td>
                                </tr>
                            </table>
                        </div>
                        <% } %> <% } %>
                    </div>
                    <!-- created team end  -->
                    <hr />
                    <!-- Joined Team -->
                    <h2 class="join-heading">Joined Teams</h2>
                    <div class="reg-container">
                        <% if(joined_teams.length == 0) {%>
                        <button type="button" class="btn btn-info">
                            No Team Joined!
                        </button>

                        <%}else {%> <% for( let index = 0; index <
                        joined_teams.length; index++ ) { %>

                        <!-- event -->
                        <div class="card-event">
                            <table>
                                <tr>
                                    <td>Event</td>
                                    <td><%= joined_teams[index].id %></td>
                                </tr>
                                <tr>
                                    <td>Team</td>
                                    <td><%= joined_teams[index].name %></td>
                                </tr>
                            </table>
                        </div>
                        <% } %> <% } %>
                    </div>
                    <!-- Joined teams end  -->
                    <hr />
                </main>
            </section>
            <%- include('partials/navbar_menu.ejs') %>
        </div>

        <script src="./js/script.js" type="text/javascript"></script>
    </body>
</html>
